<% if @documents.any? %>
  <ul class="documents" id="list_doc">
    <%= render @documents %>
  </ul>
  <%= will_paginate @documents,
    params: { controller: :static_pages, action: :home } %>
<% end %>
