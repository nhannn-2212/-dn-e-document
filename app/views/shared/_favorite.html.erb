<%= form_for(current_user.favorites.build, html: {class: "form-inline"}, remote: true) do |f| %>
  <%= hidden_field_tag :document_id, doc_id %>
  <%= button_tag( :class => "button_green") do %>
    <i class="fa fa-star"></i>
  <% end %>
<% end %>
